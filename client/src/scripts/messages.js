const socket = io('/');
const ext = JSON.parse(localStorage.getItem('ext'));
const { type, room, chats, extusers, user } = ext;

class FactorSocketChats {
    static getSnowfalkes () {
        
    }
}